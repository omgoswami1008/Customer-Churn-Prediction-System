# ---------------------------------------------------------------------------
# Customer Churn Prediction System — dependencies
#
# Versions are intentionally flexible (>=) so pip can resolve prebuilt
# wheels for whichever Python version Streamlit Community Cloud uses.
# Locally verified against: Python 3.13 · streamlit 1.51 · pandas 2.3 ·
# numpy 2.3 · scikit-learn 1.7.
# ---------------------------------------------------------------------------

streamlit>=1.40,<2
pandas>=2.1,<3
numpy>=1.26,<3
scikit-learn>=1.4,<2
matplotlib>=3.8
seaborn>=0.13
joblib>=1.3
openpyxl>=3.1
